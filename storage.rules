rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read of logos and uploaded assets, allow write for authenticated users/admins
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
