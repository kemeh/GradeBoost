rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Basic rules for debugging - allowing read/write for authenticated users
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
